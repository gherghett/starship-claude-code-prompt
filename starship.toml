# Add this to your ~/.config/starship.toml

[custom.claude]
command = "claude-session-info"
when = "claude-session-info"
format = "[$output]($style) "
style = "bold #da7756"
shell = ["bash", "--noprofile", "--norc"]
